extends Area2D

# This allows you to type a name in the Inspector (e.g., "bubble_sort_step_1")
@export var zone_id: String = ""


func _on_input_event(viewport: Node, event: InputEvent, shape_idx: int) -> void:
	pass # Replace with function body.
